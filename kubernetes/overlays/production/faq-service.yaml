apiVersion: apps/v1
kind: Deployment
metadata:
  name: faq-service
  namespace: qa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: faq-service
  template:
    metadata:
      labels:
        app: faq-service
    spec:
      containers:
      - name: faq-service
        image: 465776424604.dkr.ecr.us-east-1.amazonaws.com/student-support/faq-service:e0368900c0a4618386583526c3dfa302a81211c3
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: faq-service
  namespace: qa
spec:
  selector:
    app: faq-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP 